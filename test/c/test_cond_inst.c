int main()
{
    int x, z = 215, r = 450;
    register int y, s;

    if (x == 0)
        y = z;

    if (r != 0)
        s = r;

    puti(y);
    putc('\n');

    puti(s);
    putc('\n');

    return 0;
}
